---
apiVersion: v1
kind: Service
metadata:
  name: rk-llama-server
  namespace: rk-llama
  labels:
    app.kubernetes.io/name: rk-llama-server
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: rk-llama-server
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30808
