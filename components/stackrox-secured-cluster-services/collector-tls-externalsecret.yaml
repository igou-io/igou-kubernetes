---
# One of the three cluster-init-bundle secrets (see sensor-tls + README).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: collector-tls
  namespace: stackrox
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,SkipHealthCheck=true
    argocd.argoproj.io/sync-wave: '-2'
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-lab-openshift
  target:
    name: collector-tls
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        conversionStrategy: Default
        decodingStrategy: None
        key: stackrox-collector-tls
        metadataPolicy: None
        nullBytePolicy: Ignore
